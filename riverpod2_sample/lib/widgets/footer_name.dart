import 'package:flutter/cupertino.dart';

class FooterName extends StatelessWidget {
  const FooterName({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Your name");
  }
}
