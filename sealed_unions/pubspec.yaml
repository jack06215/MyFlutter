name: sealed_unions
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: '>=2.15.1 <3.0.0'

dependencies:
  path: ^1.8.0
  json_serializable: ^6.0.1
  freezed_annotation:

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  freezed: ^2.0.3
  build_runner:
